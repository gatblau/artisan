# Artisan Execution Flows

A library of execution flows to create pipelines.

An execution flow defines a sequence of package/runtime combinations to run and can be thought of as a logical pipeline.

