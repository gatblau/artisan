---
name: Java Application Package To Image Flow
description: build, sign and publish a Java Application Container Image

steps:
  # use case 3: call the build-image function in the APP_IMAGE_BUILD_PACKAGE
  - name: build-image
    description: builds, signs and publish the container image
    runtime: quay.io/gatblau/art-rhctools
    package: ${APP_IMAGE_BUILD_PACKAGE}
    function: build-image
...