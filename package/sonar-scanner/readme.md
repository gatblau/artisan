# SonarQube Scanner Package

Scans source code using the community [sonar scanner](https://docs.sonarqube.org/latest/analysis/scan/sonarscanner/).

## Usage

```bash
art exec ${PACKAGE_NAME} scan-app -u=${ART_REG_USER}:${ART_REG_PWD} -p=/keys/root_rsa_pub.pgp
```

